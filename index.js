/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Contenedor from './src/Interfaces/contenedor';
import App from './App';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Contenedor);
